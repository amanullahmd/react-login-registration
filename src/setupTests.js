// Importing custom Jest matchers from jest-dom
// jest-dom provides a set of custom matchers that extend Jest's default functionality
// specifically for asserting on DOM nodes. This allows for more readable and meaningful
// assertions in tests, making it easier to verify the state of the DOM.
//
// With jest-dom, you can use matchers like:
// - expect(element).toHaveTextContent(/react/i) - checks if the element contains the specified text.
//
// For more information and a full list of available matchers, visit the official documentation:
// https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
